<form method="post" action="{{ path('app_project_edit', {'id': project.id}) }}">
    <input type="hidden" name="_method" value="PATCH">

    {{ form_widget(form) }}

    <button class="btn">{{ button_label|default('Update') }}</button>
</form>