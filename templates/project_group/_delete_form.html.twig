<form method="post" action="{{ path('app_project_group_delete', {'id': project_group.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ project_group.id) }}">
    <button class="btn">Delete</button>
</form>
